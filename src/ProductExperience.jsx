import { Suspense, useEffect, useMemo, useRef, useState } from 'react';
import { Canvas } from '@react-three/fiber';
import { OrbitControls, Float, useGLTF } from '@react-three/drei';
import { ArrowLeft, Box, Camera, Flame, Leaf, LoaderCircle, Moon, ScanLine, ShoppingBag, Sparkles, Sun, Utensils, Wheat } from 'lucide-react';
import { absoluteApiUrl, getProduct, getProductBusiness, trackProductEvent } from './api';
import CustomerCart from './CustomerCart';
import ProductConfigurator from './ProductConfigurator';
function DemoFoodModel(){return <Float speed={1.4} rotationIntensity={.22} floatIntensity={.35}><group rotation={[.05,-.35,0]}><mesh position={[0,.78,0]} scale={[1.65,.5,1.65]}><sphereGeometry args={[1,64,64]}/><meshStandardMaterial color="#ef9a42" roughness={.42}/></mesh><mesh position={[0,.32,0]} scale={[1.55,.17,1.55]}><cylinderGeometry args={[1,1,1,64]}/><meshStandardMaterial color="#58c56c" roughness={.72}/></mesh><mesh position={[0,.08,0]} scale={[1.42,.24,1.42]}><cylinderGeometry args={[1,1,1,64]}/><meshStandardMaterial color="#5b241d" roughness={.82}/></mesh><mesh position={[0,-.18,0]} rotation={[0,.2,0]}><cylinderGeometry args={[1.28,1.28,.14,4]}/><meshStandardMaterial color="#ffd94a"/></mesh><mesh position={[0,-.43,0]} scale={[1.48,.22,1.48]}><cylinderGeometry args={[1,1,1,64]}/><meshStandardMaterial color="#70261e"/></mesh><mesh position={[0,-.78,0]} scale={[1.6,.35,1.6]}><sphereGeometry args={[1,64,64]}/><meshStandardMaterial color="#d88635"/></mesh></group></Float>}
function GeneratedModel({url}){const gltf=useGLTF(url);return <Float speed={1.1} rotationIntensity={.12} floatIntensity={.22}><primitive object={gltf.scene} scale={2.1}/></Float>}
function Stat({icon:Icon,label,value}){return <div className="product-stat glass-panel"><Icon size={18}/><div><span>{label}</span><strong>{value}</strong></div></div>}
export default function ProductExperience({onBack,productId:propProductId}){const[mode,setMode]=useState('3d'),[product,setProduct]=useState(null),[business,setBusiness]=useState(null),[loadError,setLoadError]=useState(''),[cartOpen,setCartOpen]=useState(false),[configuration,setConfiguration]=useState(null),[daylight,setDaylight]=useState(()=>localStorage.getItem('ashes_consumer_theme')==='daylight');const tableCode=useMemo(()=>new URLSearchParams(window.location.search).get('table')||'',[]);const arViewerRef=useRef(null),trackedScanRef=useRef(false),tracked3DRef=useRef(false);const productId=useMemo(()=>propProductId||new URLSearchParams(window.location.search).get('product'),[propProductId]);useEffect(()=>{if(!productId)return;let cancelled=false,timer;if(!trackedScanRef.current){trackedScanRef.current=true;trackProductEvent(productId,'scan').catch(()=>{})}const load=async()=>{try{const[data,merchant]=await Promise.all([getProduct(productId),getProductBusiness(productId)]);if(!cancelled){setProduct(data);setBusiness(merchant);setLoadError('')}if(!cancelled&&['queued','processing'].includes(data.status))timer=setTimeout(load,2500)}catch(e){if(!cancelled){setProduct(null);setBusiness(null);setLoadError(e?.message||'Product could not be loaded')}}};load();return()=>{cancelled=true;clearTimeout(timer)}},[productId]);useEffect(()=>{if(!productId||mode!=='3d'||tracked3DRef.current)return;tracked3DRef.current=true;trackProductEvent(productId,'view_3d').catch(()=>{})},[mode,productId]);const data=product||{name:'Quantum Smash Burger',price:1290,calories:'~820 kcal',protein:'42 g',carbs:'56 g',fat:'47 g',tags:['HIGH PROTEIN','HALAL'],status:productId?'loading':'demo'};const processing=['queued','processing','awaiting-generator','loading'].includes(data.status),modelUrl=data.model_url?absoluteApiUrl(data.model_url):null,modelReady=data.status==='ready'&&Boolean(modelUrl),accent=business?.accent_color||'#ff2f9f',merchantInitial=(business?.name||'A').slice(0,1).toUpperCase(),configuredPrice=Number(data.price||0)+Number(configuration?.surcharge||0);const launchAR=async()=>{if(!modelReady||!arViewerRef.current)return;if(productId)trackProductEvent(productId,'ar_launch').catch(()=>{});try{await arViewerRef.current.activateAR()}catch{setMode('ar')}};const toggleTheme=()=>setDaylight(v=>{const n=!v;localStorage.setItem('ashes_consumer_theme',n?'daylight':'night');return n});return <main className={`product-page ${daylight?'daylight':''}`} data-consumer-theme={daylight?'day':'night'} style={{'--business-accent':accent}}><div className="scan-grid"/><header className="product-topbar"><button className="icon-button" onClick={onBack}><ArrowLeft size={20}/></button><div className="brand-lockup"><div className="brand-mark">{business?.logo_url?<img src={absoluteApiUrl(business.logo_url)} alt={business.name}/>:merchantInitial}</div><div><strong>{business?.name||'ASHES AI'}</strong><span>POWERED BY ASHES AI</span></div></div><div style={{display:'flex',gap:8,alignItems:'center'}}><button className="consumer-theme-toggle" onClick={toggleTheme}>{daylight?<Moon size={15}/>:<Sun size={15}/>}<span>{daylight?'Night':'Daylight'}</span></button><button className="cart-pill" onClick={()=>setCartOpen(true)}><ShoppingBag size={16}/> Browse & order</button></div></header>{modelReady&&<model-viewer ref={arViewerRef} class="ashes-hidden-ar-viewer" src={modelUrl} ar ar-modes="webxr scene-viewer quick-look" camera-controls shadow-intensity="1" exposure="1" alt={data.name}/>}<section className="product-experience-shell"><div className="product-visual glass-panel neon-edge"><div className="visual-toolbar"><div className="eyebrow"><Sparkles size={15}/> {modelReady?'AI GENERATED PRODUCT TWIN':'ASHES PRODUCT EXPERIENCE'}</div><div className="mode-switch"><button className={mode==='3d'?'active':''} onClick={()=>setMode('3d')}><Box size={16}/> 3D</button><button className={mode==='ar'?'active':''} onClick={()=>setMode('ar')}><Camera size={16}/> AR</button></div></div><div className="canvas-wrap">{mode==='3d'?<Canvas camera={{position:[0,0,5.8],fov:40}}><ambientLight intensity={1.6}/><pointLight position={[4,5,4]} intensity={28} color={accent}/><pointLight position={[-4,1,3]} intensity={22} color="#21e8ff"/><Suspense fallback={null}>{modelReady?<GeneratedModel url={modelUrl}/>:<DemoFoodModel/>}</Suspense><OrbitControls enablePan={false} minDistance={3.3} maxDistance={9} autoRotate autoRotateSpeed={.7}/></Canvas>:<div className="ar-placeholder"><div className="ar-reticle"><ScanLine size={54}/></div><h3>{modelReady?'Place it in your space':'Preparing AR asset'}</h3><p>{modelReady?'View this product through your device AR viewer.':'AR unlocks when the 3D asset is ready.'}</p><button className="primary-action" disabled={!modelReady} onClick={launchAR}><Camera size={18}/> LAUNCH AR</button></div>}{processing&&<div className="floating-chip chip-one"><LoaderCircle size={13} className="spin-icon"/> GENERATING 3D</div>}<div className="floating-chip chip-two">REAL-TIME 3D</div><div className="floating-chip chip-three">DRAG TO ROTATE</div></div></div><aside className="product-info"><div className="merchant-line"><div className="merchant-logo">{business?.logo_url?<img src={absoluteApiUrl(business.logo_url)} alt={business.name}/>:merchantInitial}</div><div><span>Now viewing</span><strong>{business?.name||'Ashes Partner'}</strong></div></div><div className="product-heading"><div className="eyebrow pink">ASHES EXPERIENCE</div><h1>{data.name}</h1><p>{processing?'Ashes is preparing the interactive 3D twin.':`Configure this item, see its live price, then add the exact version to your order.`}</p></div><div className="price-row"><strong>Rs {configuredPrice.toLocaleString()}</strong><span>{configuration?.surcharge>0?`Base Rs ${Number(data.price||0).toLocaleString()}`:(tableCode?`Table ${tableCode}`:business?.city||'Scan experience')}</span></div>{product&&<ProductConfigurator product={product} onChange={setConfiguration}/>}<div className="stats-grid"><Stat icon={Flame} label="Calories" value={data.calories||'—'}/><Stat icon={Utensils} label="Protein" value={data.protein||'—'}/><Stat icon={Leaf} label="Carbs" value={data.carbs||'—'}/><Stat icon={Wheat} label="Fat" value={data.fat||'—'}/></div><div className="tag-row">{(data.tags||[]).map(t=><span key={t}>{t}</span>)}</div>{loadError&&<div className="allergen-note glass-panel"><strong>Product loading</strong><p>{loadError}</p></div>}<div className="cta-stack"><button className="primary-action wide" disabled={!modelReady} onClick={launchAR}><Camera size={19}/> VIEW CONFIGURED PRODUCT IN AR</button><button className="secondary-action wide" onClick={()=>setCartOpen(true)}><ShoppingBag size={19}/> ADD / BROWSE & ORDER · Rs {configuredPrice.toLocaleString()}</button></div></aside></section>{cartOpen&&business&&<CustomerCart business={business} initialProduct={product} initialConfiguration={configuration} tableCode={tableCode} onClose={()=>setCartOpen(false)} onViewProduct={id=>{setCartOpen(false);window.location.assign(`/?product=${encodeURIComponent(id)}${tableCode?`&table=${encodeURIComponent(tableCode)}`:''}`)}}/>}</main>}
